# AI orchestration

The model receives a project synopsis, structural summaries, selected views, and only relevant exact slices/chunks. Every item has an evidence ID. A budget planner reserves output/tool tokens and limits images. Model output is untrusted: all tools are schema-validated, authorized, preconditioned, bounded, and executed server-side.
