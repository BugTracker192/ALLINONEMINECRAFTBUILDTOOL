# Snapshots

Every snapshot is immutable and linked to build content hash, renderer version, asset-pack hash, coordinate space, visible bounds, camera matrices, clipping/material filters, resolution, lighting preset, and image hash. Current code generates exact Y palette maps and reversible pixel/block transforms. The headless service is designed to reuse the same renderer package.
