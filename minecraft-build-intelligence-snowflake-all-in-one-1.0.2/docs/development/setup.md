# Development setup

Requirements: Python 3.12+, uv, Node 22.12+, pnpm 10, PostgreSQL, Redis, and optionally Docker. `uv sync --all-packages --extra test`, `pnpm install`, then run the API and web commands in the root README. Keep source fixtures generated and redistributable.
