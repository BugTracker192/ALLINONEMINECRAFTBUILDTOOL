# Importing a build

Choose `.schem`, `.schematic`, or `.litematic`. The application hashes and streams the upload, detects compression and format by bytes/tags, parses within limits, reports warnings, canonicalizes coordinates/states, chunks the build, and starts preview loading. Legacy unresolved states remain visible placeholders until mapped.
