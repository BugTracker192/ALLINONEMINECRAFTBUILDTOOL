apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: mbi
resources:
  - namespace.yaml
  - config-and-storage.yaml
  - api-deployment.yaml
  - web-deployment.yaml
  - worker-deployment.yaml
  - renderer-deployment.yaml
  - network-policy.yaml
# Intentionally exclude secrets.example.yaml. Supply mbi-api-secrets through a
# sealed-secret/external-secret workflow or another deployment-only mechanism.
