# Security Policy

Do not open public issues for vulnerabilities. Send a minimal report to the private maintainer contact configured by the deployment owner.

Uploads, resource packs, NBT, and AI tool calls are untrusted. The production deployment must enforce worker CPU, memory, process, filesystem, network, and wall-clock limits. Secrets must remain server-side and be redacted from logs and traces.
